Dans les menus:
    Options :
        ->Parametre de l'heure :
                - connecter langue
                - connecter edit-date
                - connecter edit-time
        ->Affichage :
                - implementer type numerique
                - connecter type horloge
                - implementer forme traits
                - connecter forme aiguilles
                - implementer fond image
                - connecter transparence
        ->Theme :
                - implementer et connecter binaire
                - implementer et connecter hexadecimal
                - implementer et connecter old school
                - implementer et connecter matrix
                - implementer et connecter 8 bits
                - implementer et connecter type chrono analogique
                - implementer et connecter type chrono numerique
    Reveil :
        ->nouveau-reveil :
                - implementer et connecter jours-de-la-semaines
                - implementer et connecter repetition
                - implementer et connecter niveau du volume
                - enregistrement d'une liste de fichier audio(et non un fichier pour tout les reveils)
    Minuteur :
        ->implementer sauvegarde des minuteurs

Tache de fond :
        - Implementer l'execution automatique de l'application au demmarage de l'ordinateur,
    automatiser la configuration ( e.g.premiere execution -> creation d'un fichier ini)
        - Corriger la transparence en mode fenetre (rendre opaque ou d'opacite configuree dans les settings)
        - enregistrer la position et la taille de l'application et repositionner au demmarage

Fonctionnalites a ajouter :
        - choix de garder l'application en premier plan ou non
        -

Pour les developpeurs :
        - completer les commentaires doxygen
        - ajouter la generation automatique de documentation doxygen au projet
        - (optionel) creer une page css pour personnaliser la presentation de la documentation

Projet General :
        ->porter le projet sur android :
                - finir de configurer le compilateurs
        ->Remplir le fichier README.md
        ->Ajouter les licenses

